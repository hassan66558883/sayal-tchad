<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_seyal_expense_tree" model="ir.ui.view">
        <field name="name">seyal.expense.tree</field>
        <field name="model">seyal.expense</field>
        <field name="arch" type="xml">
            <tree string="Depenses" decoration-muted="state == 'cancelled'">
                <field name="reference"/>
                <field name="category"/>
                <field name="branch_id"/>
                <field name="expense_date"/>
                <field name="amount" sum="Total"/>
                <field name="state" widget="badge"/>
            </tree>
        </field>
    </record>

    <record id="view_seyal_expense_form" model="ir.ui.view">
        <field name="name">seyal.expense.form</field>
        <field name="model">seyal.expense</field>
        <field name="arch" type="xml">
            <form string="Depense">
                <header>
                    <button name="action_confirm" type="object" string="Confirmer" class="oe_highlight"
                            invisible="state != 'draft'"/>
                    <button name="action_cancel" type="object" string="Annuler"
                            invisible="state != 'draft'"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,confirmed"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <span class="o_form_label text-muted">Reference</span>
                        <h1><field name="reference" readonly="1"/></h1>
                    </div>
                    <group>
                        <group string="Informations generales">
                            <field name="category"/>
                            <field name="branch_id"/>
                            <field name="expense_date"/>
                            <field name="amount"/>
                        </group>
                        <group string="Imputation">
                            <field name="cash_account_id"/>
                            <field name="bank_account_id"/>
                        </group>
                    </group>
                    <group>
                        <field name="description" nolabel="1"/>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="action_seyal_expense" model="ir.actions.act_window">
        <field name="name">Depenses</field>
        <field name="res_model">seyal.expense</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>
