# Copier ce fichier en ".env" et renseigner de vraies valeurs.
# Ne jamais committer le fichier ".env" (pas de secrets dans le code source).

DB_USER=odoo
DB_PASSWORD=changeme_use_a_strong_password
ODOO_ADMIN_PASSWORD=changeme_master_password

# Uniquement pour la production (docker-compose.prod.yml) - voir README,
# section "Deploiement en production (HTTPS)".
DOMAIN=erp.exemple-seyal.td
CERTBOT_EMAIL=admin@exemple-seyal.td
